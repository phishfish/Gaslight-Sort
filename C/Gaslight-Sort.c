#include <stdio.h>
#include <stdbool.h>

bool isSorted(int *array)
{
	return true;
}

int main(void)
{
	int numInts;

	printf("How many integers to be sorted?\n");
	scanf("%d", &numInts);
	
	return 0;
}
